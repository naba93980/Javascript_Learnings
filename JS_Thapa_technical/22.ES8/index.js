/*const mssg = "nabajyoti";
console.log( mssg.padStart(20) ); //padding gives space at beggining
console.log(mssg.padEnd(20));//padding here gives space at the end
*/
const person = {
    myName:"naba",
    myAge:22,
}
console.log( Object.keys(person) ); //[ 'myName', 'myAge' ]
console.log( Object.values(person) );//[ 'naba', 22 ]
console.log( Object.entries(person) );//[ [ 'myName', 'naba' ], [ 'myAge', 22 ] ]

